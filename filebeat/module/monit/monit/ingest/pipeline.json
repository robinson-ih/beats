{
  "description": "Pipeline for parsing monit monit logs",
  "processors": [
  	{
    "grok": {
      "field": "message",
      "patterns":[
        "%{MONIT_TIMESTAMP:monit.timestamp}.*%{LOGLEVEL}.*%{QUOTEDSTRING:worker}.*pid.*%{NUMBER:pid}",
        "%{MONIT_TIMESTAMP:monit.timestamp}.*%{LOGLEVEL}.*%{QUOTEDSTRING:worker}%{GREEDYDATA}"
        ],
       "pattern_definitions": {
          "MONIT_TIMESTAMP": "%{TZ} %{MONTH} %{MONTHDAY} %{TIME}"
        },
      "ignore_missing": true
    }
   },
    {
      "date": {
        "field": "monit.timestamp",
        "target_field": "@timestamp",
       "formats": [
	  "MMM  d HH:mm:ss",
	  "MMM dd HH:mm:ss"
        ],
       "ignore_failure": true
       }
    }
  ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}